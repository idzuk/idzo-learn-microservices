package ua.idzo.resource.core.integration.processor;

import ua.idzo.resource.core.entity.ResourceEntity;

public interface ResourceProcessor {

    void processResource(ResourceEntity resourceEntity);
}
